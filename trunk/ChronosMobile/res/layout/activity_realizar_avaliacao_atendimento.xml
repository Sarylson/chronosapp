<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/app_background"
    android:orientation="vertical"
    android:padding="@dimen/padding_large" >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/borda_1"
        android:orientation="vertical"
        android:padding="@dimen/padding_large" >

        <ListView
            android:id="@+id/listViewTipoAvaliacao"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <ListView
            android:id="@+id/listViewItemAvaliacao"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <TextView
            android:layout_width="wrap_content"
             android:layout_marginTop="15dp"
            android:layout_height="wrap_content"
            android:text="@string/lb_avaliacao_atendimento_descricao"
            android:textSize="16sp" />

        <EditText
            android:id="@+id/txtDescricaoAvaliacao"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/text_default"
            android:gravity="top|left"
            android:inputType="textMultiLine"
            android:lines="7"
            android:padding="@dimen/padding_small"
            android:scrollHorizontally="false"
            android:maxLength="400" />

        <Button
            android:id="@+id/btn_confirmar_avaliacao_atendimento"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="5dp"
            android:background="@drawable/button_confirmar_login"
            android:onClick="confirmarAvaliacaoAtendimento"
            android:padding="@dimen/padding_small"
            android:text="@string/lb_logar"
            android:textColor="#FFF"
            android:textSize="16sp"
            android:textStyle="bold" />
    </LinearLayout>

</LinearLayout>